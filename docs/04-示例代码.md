# 04-示例代码

本页只列出能在当前仓库中真实定位到的示例、测试和运行命令。每一项都给出：

- 源文件路径
- CMake 目标名
- 推荐运行命令
- 需要的环境变量或外部基础设施

下文统一用 `<build-dir>` 表示你的 CMake binary dir；它可以是 `build`、`build-release` 或其他自定义目录。

## 示例二进制

这些目标来自 `examples/CMakeLists.txt`。

### include 版本

| 场景 | 源文件 | 目标 | 运行命令 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| 基础 async SET/GET/DEL | `examples/include/e1_basic.cc` | `e1_basic` | `./<build-dir>/examples/e1_basic 127.0.0.1 6379 demo:key demo-value` | 无 | 最短主路径示例 |
| batch / pipeline 风格发送 | `examples/include/e2_pipeline.cc` | `e2_pipeline` | `./<build-dir>/examples/e2_pipeline 127.0.0.1 6379 demo:pipeline: 20` | 无 | 演示 `append` + `batch(commands())` |
| Pub/Sub + topology API 形状 | `examples/include/e3_pubsub.cc` | `e3_pubsub` | `./<build-dir>/examples/e3_pubsub 127.0.0.1 6379` | 无 | 在单节点上演示 API，不等价于真实 Sentinel/Cluster 部署 |

### import 版本

以下目标只有在 `galay-redis-modules` 可用时才会生成：

| 场景 | 源文件 | 目标 | 运行命令 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| 基础 async SET/GET/DEL | `examples/import/e1_basic.cc` | `e1_basic-import` | `./<build-dir>/examples/e1_basic-import 127.0.0.1 6379 demo:key demo-value` | 无 | 对齐 `e1`，验证 `import galay.redis;` |
| batch / pipeline 风格发送 | `examples/import/e2_pipeline.cc` | `e2_pipeline-import` | `./<build-dir>/examples/e2_pipeline-import 127.0.0.1 6379 demo:pipeline: 20` | 无 | 对齐 `e2`，验证模块消费路径 |
| Pub/Sub + topology API 形状 | `examples/import/e3_pubsub.cc` | `e3_pubsub-import` | `./<build-dir>/examples/e3_pubsub-import 127.0.0.1 6379` | 无 | 对齐 `e3`，验证模块消费路径 |

## 测试即文档样例

这些目标来自 `test/CMakeLists.txt`，比 README 中的伪代码更接近真实行为。

| 场景 | 源文件 | 目标 | 运行命令 | 环境变量 | 说明 |
|---|---|---|---|---|---|
| 基础异步命令 | `test/t1_async.cc` | `t1_async` | `./<build-dir>/test/t1_async` | 无 | 连接、SET、GET、DEL |
| timeout 行为 | `test/t5_timeout.cc` | `t5_timeout` | `./<build-dir>/test/t5_timeout` | 无 | `.timeout(...)` 用法 |
| 全命令族覆盖 | `test/t6_commands.cc` | `t6_commands` | `./<build-dir>/test/t6_commands` | 无 | 验证 `builder.h` 便捷方法 |
| batch timeout API 形状 | `test/t8_batch.cc` | `t8_batch` | `./<build-dir>/test/t8_batch` | 无 | 不依赖真实 Redis |
| batch span 复用 | `test/t9_span.cc` | `t9_span` | `./<build-dir>/test/t9_span` | 无 | 验证 `commands()` 与 `clear()` |
| raw command API | `test/t10_command.cc` | `t10_command` | `./<build-dir>/test/t10_command` | 无 | `builder.release()` / `builder.command(...)` |
| awaitable / pool surface 回归 | `test/t15_surface.cc` | `t15_surface` | `./<build-dir>/test/t15_surface` | 无 | 验证 awaitable 精简和连接池 surface |
| RESP encoder 估算 | `test/t20_encoder.cc` | `t20_encoder` | `./<build-dir>/test/t20_encoder` | 无 | 编码尺寸估算回归 |
| 导出接口合同 | `test/interface/verify_interface.cmake.in` | `t24_interface_contract` | `ctest --test-dir <build-dir> --output-on-failure -R '^t24_interface_contract$'` | 无 | 检查导出目标不泄漏内核 include 与 `galay-utils` |
| `rediss://` surface | `test/t16_rediss.cc` | `t16_rediss` | `./<build-dir>/test/t16_rediss` | 需以 `GALAY_REDIS_ENABLE_SSL=ON` 构建 | TLS facade / Task surface 编译回归 |
| TLS 单节点 smoke | `test/t17_tls.cc` | `t17_tls` | `GALAY_REDIS_TLS_URL=rediss://... ./<build-dir>/test/t17_tls` | `GALAY_REDIS_TLS_URL`，可选 `GALAY_REDIS_TLS_CA` / `GALAY_REDIS_TLS_VERIFY_PEER` / `GALAY_REDIS_TLS_SERVER_NAME` | 可连接真实 TLS Redis 时执行，否则按 `SKIP` 退出 |
| topology + pubsub | `test/t11_pubsub.cc` | `t11_pubsub` | `./<build-dir>/test/t11_pubsub` | 无 | 单机 Redis 上验证 API 主路径 |
| topology single-flight | `test/t12_topology.cc` | `t12_topology` | `./<build-dir>/test/t12_topology` | 无 | 不依赖真实 Redis |
| real cluster + sentinel 集成 | `test/t13_cluster.cc` | `t13_cluster` | `./<build-dir>/test/t13_cluster` | 见下表 | 真实 MOVED/ASK/failover |
| TLS pool + topology smoke | `test/t19_topology.cc` | `t19_topology` | `GALAY_REDIS_TLS_URL=rediss://... ./<build-dir>/test/t19_topology` | `GALAY_REDIS_TLS_URL`，可选 TLS Sentinel / Cluster 环境变量 | 覆盖 `RedissConnectionPool`、`RedissMasterSlaveClient`、`RedissClusterClient` |

## 标准 CTest 入口

`test/CMakeLists.txt` 现在把这些目标注册进了标准 CTest 矩阵，并按依赖打上标签：

- `unit`：纯本地离线测试，如 `t3`、`t8`、`t9`、`t10`、`t12`、`t14`、`t15`、`t16`、`t20`、`t24_interface_contract`
- `redis`：Redis 仓库的常规测试标签，既包含离线单测，也包含依赖本地 `127.0.0.1:6379` 的网络测试
- `integration`：真实 Cluster + Sentinel 集成测试；若未设置 `GALAY_IT_ENABLE=1`，会按 `[SKIP]` 语义跳过

常用命令：

```bash
ctest --test-dir <build-dir> --output-on-failure -L unit
ctest --test-dir <build-dir> --output-on-failure -L redis
GALAY_IT_ENABLE=1 ctest --test-dir <build-dir> --output-on-failure -L integration
```

## `t13_cluster` 环境要求

真实集成测试的入口有两个：

| 入口 | 命令 | 说明 |
|---|---|---|
| 直接运行目标 | `./<build-dir>/test/t13_cluster` | 需要手动准备 cluster + sentinel 环境 |
| 使用辅助脚本 | `test/integration/run_cluster_sentinel_integration.sh --build-dir <build-dir>` | 自动拉起 Docker Compose，构建目标，并设置环境变量 |

辅助脚本支持：

- `--build-dir <build-dir>` 或 `GALAY_BUILD_DIR=<build-dir>`
- `--binary <path>` 或 `GALAY_IT_BINARY=<path>`（直接指定现成二进制）
- 未显式传参时，优先尝试仓库根目录下的 `build`，再扫描已有的 `build*` 目录

脚本与测试会读取这些环境变量：

- `GALAY_IT_ENABLE`
- `GALAY_IT_CLUSTER_HOST`
- `GALAY_IT_CLUSTER_PORT`
- `GALAY_IT_MOVED_KEY`
- `GALAY_IT_ASK_KEY`
- `GALAY_IT_SENTINEL_HOST`
- `GALAY_IT_SENTINEL_PORT`
- `GALAY_IT_SENTINEL_MASTER_NAME`
- `GALAY_IT_SENTINEL_KEY`
- `GALAY_IT_TRIGGER_SENTINEL_FAILOVER`

## 如何选择事实来源

如果你要给团队或 AI 系统提供示例检索，建议优先级如下：

1. `examples/include/e1_basic.cc`
2. `examples/include/e2_pipeline.cc`
3. `test/t5_timeout.cc`
4. `test/t10_command.cc`
5. `test/t11_pubsub.cc`
6. `test/t13_cluster.cc`
7. `test/t15_surface.cc`（当你要检索 awaitable / pool public surface 时，优先看它）

如果你要验证模块消费路径，则把对应的 `examples/import/e1`~`e3` 放到与 include 示例同级参考。

原因：

- 它们覆盖了当前 async 主路径
- 文件名和目标名一一对应
- 命令路径可直接落到 `examples/CMakeLists.txt` / `test/CMakeLists.txt`

## 与 benchmark 的边界

示例页不承载性能结论。要查看真实 benchmark 目标名、CLI 参数和“结果是否当前”的声明，请转到 [05-性能测试](05-性能测试.md)。
